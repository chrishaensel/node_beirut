#Beirut

Gets country data for Beirut, Lebanon.

Usage

####Get country data
```
const beirut = require('beirut)
const data = beirut.getData()
```


####Get current weather data
```
const beirut = require('beirut)
const weather = beirut.getWeather(function(wx) {
    console.log(wx)
})
```

